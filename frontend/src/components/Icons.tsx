export {};

// import Icon from "@ant-design/icons";

// export type IconNames = "Incident" | "Patrol";

// export interface IconProps {
//   name: IconNames;
// }

// const EmojiToIcon = (emoji: string) => <Icon>{emoji}</Icon>;

// export const CustomIcon = ({ name }: IconProps) => {
//   switch (name) {
//     case "Incident":
//       return EmojiToIcon("❗");
//     case "Patrol":
//       return EmojiToIcon("🚓");
//   }
// };

// TODO Remove
